<% if (flashMessage ) { %>
  <div class="alert alert-<% if (isSuccessFlashMessage) { %>success<% } else { %>danger<% }  %>" role="alert">
    <%= flashMessage %> 
    <button type="button" class="btn-close float-end" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% } %>
